# Journey V2 — protected stars copy

- Verified the original `src/components/canvas/Stars.jsx` is untouched.
- Created a journey-owned copy of both `Stars` and `StarsCanvas`.
- Kept the original sphere, camera, point size, pink color, and rotation behavior.
- Switched the active resting background to the journey-owned copy.
- Future star experiments can target the copy without changing the legacy original.
